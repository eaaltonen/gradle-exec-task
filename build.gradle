defaultTasks 'makeFile'

def output = 'file'

task clean(type:Delete) {
    delete output
}

task makeFile(type:PrintExec) {
    group 'build'
    printCmd true // or 1
//    executable 'echo'
//    args = ['echoing']
    commandLine 'echo', "command line"
}
